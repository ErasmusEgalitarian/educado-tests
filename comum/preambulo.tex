\usepackage[brazil]{babel}
% Recuo tambem no primeiro paragrafo de cada secao (norma brasileira).
\usepackage{indentfirst}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}

\usepackage{geometry}
\geometry{
    a4paper,
    top=2.5cm,
    bottom=2.5cm,
    left=3cm,
    right=2cm
}

\usepackage{graphicx}
\usepackage{float}
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}
\usepackage{tabularx}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{hyperref}

% Citações e referências em ABNT autor-data (NBR 10520 e 6023).
% \cite{chave} produz (AUTOR, ano); \citeonline{chave} produz Autor (ano).
\usepackage[alf,abnt-emphasize=bf]{abntex2cite}
\bibliographystyle{abntex2-alf}
\usepackage{listings}
\usepackage{microtype}
\usepackage{setspace}

% ============================================================================
% CONFIGURAÇÕES
% ============================================================================

\hypersetup{
    colorlinks=true,
    linkcolor=black,
    citecolor=black,
    urlcolor=blue,
    pdftitle={Relatório de Estratégia de Testes},
    pdfauthor={}
}

\onehalfspacing

\setlength{\parindent}{1.25cm}
\setlength{\parskip}{0pt}

\setlist[itemize]{noitemsep,topsep=4pt}
\setlist[enumerate]{noitemsep,topsep=4pt}

% ============================================================================
% CÓDIGO-FONTE
% ============================================================================

\definecolor{codegray}{gray}{0.95}

\lstset{
    backgroundcolor=\color{codegray},
    basicstyle=\ttfamily\small,
    breaklines=true,
    frame=single,
    numbers=left,
    numberstyle=\tiny,
    showstringspaces=false,
    tabsize=2
}

% ============================================================================
% ADIÇÕES DA EQUIPE (não fazem parte do template original da disciplina)
%
% Integrantes aparecem na capa E na identificação do projeto. Escrever nos
% dois lugares é convite pra divergir, então o dado mora só em equipe.tex de
% cada relatório e cada lugar decide como renderizar.
% ============================================================================

% Placeholder: cada contexto redefine antes de chamar \listaintegrantes.
\newcommand{\integrante}[2]{}
